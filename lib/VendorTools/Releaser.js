const {
    AsymmetricSecretKey,
    AsymmetricPublicKey,
    Keyring
} = require('dhole-crypto');

/**
 * Utility for signing and releasing new versions of a project
 *
 * @type {module.Releaser}
 */
module.exports = class Releaser {

};
