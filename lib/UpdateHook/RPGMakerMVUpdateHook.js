const UpdateHook = require('../UpdateHook');

/**
 * Update hooks for RPG Maker MV projects.
 *
 * Will support hooks for:
 *
 * - Updating the RPG Maker MV project database.
 * - Updating the scripts files.
 *
 * @type {module.RPGMakerMVUpdateHook}
 */
module.exports = class RPGMakerMVUpdateHook extends UpdateHook {

};
